apiVersion: apps/v1
kind: Deployment
metadata:
  name: quiz-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quiz-mongo
  template:
    metadata:
      labels:
        app: quiz-mongo
    spec:
      containers:
      - name: mongo
        image: mongo:7
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: quiz-mongo
spec:
  selector:
    app: quiz-mongo
  ports:
  - port: 27017
    targetPort: 27017
